import React from 'react';
import { View } from 'react-native';

export default function ActionScreen() {
  // Esta tela não é renderizada, apenas serve como placeholder
  // para o botão flutuante na navegação
  return <View />;
} 